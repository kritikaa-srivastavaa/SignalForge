# Optional, explicit local admin bootstrap. Keep disabled during normal operation.
SIGNALFORGE_BOOTSTRAP_ENABLED=false
SIGNALFORGE_BOOTSTRAP_EMAIL=
SIGNALFORGE_BOOTSTRAP_PASSWORD=
SIGNALFORGE_BOOTSTRAP_DISPLAY_NAME=Local Administrator
# Supply a new email and temporary password locally; never commit real credentials.
