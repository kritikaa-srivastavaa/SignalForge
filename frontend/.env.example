# Host Vite development: npm run dev (Node 24).
VITE_API_BASE_URL=http://localhost:8080
# Docker builds default to /api through the Dockerfile build argument.
# Vite embeds the value at build time; container runtime variables do not change it.
